<?xml version="1.0" encoding="utf-8"?>
<odoo> 

<!-- 
 issue_type = fields.Selection([
        ('plumbing', 'Plumbing'),
        ('electrical', 'Electrical'),
        ('air_condition', 'Air Condition'),
        ('appliance', 'Appliance'),
        ('other', 'Other')
    ], required=True)
    description = fields.Text(required=True, tracking=True)
    state = fields.Text(required=True, tracking=True)
    urgency = fields.Selection([
        ('low', 'Low'),
        ('medium', 'Medium'),
        ('high', 'High'),
        ('emergency', 'Emergency')
    ], default='medium', required=True)
    assigned_to = fields.Many2one('res.users', string='Assigned To')
    scheduled_date = fields.Date()
    completion_date = fields.Date()
    actual_cost = fields.Float() -->
<template id="portal_my_maintenance_requests" name="Tenant Portal - My Maintenance Requests">
    <t t-call="portal.portal_layout">

        <div class="container mt16 o_portal_portlets">

            <div class="row">
                <div class="col-12">
                    <nav class="breadcrumb" aria-label="breadcrumb">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item">
                                <a href="/my/home">My Account</a>
                            </li>
                            <li class="breadcrumb-item active" aria-current="page">
                                My Maintenance Requests
                            </li>
                        </ol>
                    </nav>

                    <h1 class="mb16">My Maintenance Requests</h1>
                </div>
            </div>

            <t t-if="maintenance_requests">
                <div class="row">

                    <t t-foreach="maintenance_requests" t-as="main_request">

                        <div class="col-md-6 mb16">
                            <div class="card h-100">
                                <div class="card-body">

                                    <h5 class="card-title mb-2">
                                        <t t-esc="main_request.name"/>
                                    </h5>

                                    <p class="text-muted mb-2">
                                        <strong>Description:</strong>
                                        <t t-esc="main_request.description"/>
                                    </p>

                                    <p class="mb-1">
                                        <strong>Property Type:</strong>
                                        <t t-esc="main_request.property_id.property_type or 'N/A'"/>
                                    </p>

                                    <p class="mb-1">
                                        <strong>Issue Type:</strong>
                                        <t t-esc="main_request.issue_type or 'N/A'"/>
                                    </p>

                                    <p class="mb-1">
                                        <strong>Actual Cost:</strong>
                                        <t t-esc="main_request.actual_cost"/>
                                    </p>

                                    <p class="mb-1">
                                        <strong>Status:</strong>
                                        <t t-esc="main_request.state"/>
                                    </p>

                                    <p class="mb-1">
                                        <strong>Start:</strong>
                                        <t t-esc="main_request.scheduled_date or '—'"/>
                                    </p>

                                    <p class="mb-1">
                                        <strong>Completion Date:</strong>
                                        <t t-esc="main_request.completion_date or '—'"/>
                                    </p>

                                </div>
                            </div>
                        </div>

                    </t>

                </div>
            </t>

            <t t-if="not maintenance_requests">
                <div class="row">
                    <div class="col-12">
                        <div class="alert alert-info" role="alert">
                            No maintenance requests were found for your account.
                        </div>
                    </div>
                </div>
            </t>

        </div>

    </t>
</template>

    <template id="portal_my_maintenance_requests_menu" name="Portal My Home: My Maintenance Requests" inherit_id="portal.portal_my_home" priority="30">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="before">
            <t t-set="portal_service_category_enable" t-value="True"/>
        </xpath>
        <div id="portal_service_category" position="inside">
            <t>
                <t t-call="portal.portal_docs_entry">
                    <t t-set="icon" t-value="'/real_estate/static/src/img/wrench.svg'"/> 
                    <t t-set="title">My Maintenance Requests</t>
                    <t t-set="text">View your current maintenance requests.</t>
                    <t t-set="url" t-value="'/my/maintenance-requests'"/>
                    <t t-set="config_card" t-value="True"/>
                    <t t-set="placeholder_count" t-value="'maintenance_request_count'"/>
                </t>
            </t>
        </div>
    </template>
    </odoo>